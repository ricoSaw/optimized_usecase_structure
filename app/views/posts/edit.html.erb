<h1>Editing Post</h1>

<%= form_for(@post_form, as: :post, url: post_path(@post), method: :put) do |f| %>
  <% if @post_form.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post_form.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post_form.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
